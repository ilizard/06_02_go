    - name: Test
      run: |
        
        ./dummy-server.bin &
        SERVER_PID=$!
        echo "✅ Сервер запущен (PID: $SERVER_PID)"
        
       
        sleep 10
        if ! kill -0 $SERVER_PID 2>/dev/null; then
          echo "❌ Процесс сервера завершился. Возможные причины:"
          echo "   - Ошибка в коде сервера"
          echo "   - Конфликт порта (попробуйте изменить :8888 в main.go)"
          exit 1
        fi
        
        
        echo "🔍 Проверяем, слушает ли порт 8888..."
        if ss -tulpn | grep :8888 > /dev/null; then
          echo "✅ Порт 8888 слушается"
        else
          echo "⚠️  Процесс жив, но порт 8888 не слушается"
        fi
        
        
        echo "🧪 Тестируем подключение (3 попытки)..."
        for i in {1..3}; do
          if RESPONSE=$(curl -s -f --connect-timeout 10 http://localhost:8888 2>/dev/null); then
            echo "✅ Сервер ответил: '$RESPONSE'"
            
            kill $SERVER_PID
            wait $SERVER_PID 2>/dev/null
            exit 0
          else
            echo "   Попытка $i не удалась. Ждём 3 секунды..."
            sleep 3
          fi
        done
        
        echo "❌ Не удалось получить ответ от сервера после 3 попыток"
        
        kill -9 $SERVER_PID 2>/dev/null
        exit 1
